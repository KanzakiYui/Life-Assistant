const breakpoints = {
    values: {
        xs: 0, // >=0      (default)
        sm: 600, // >=600    (default)
        md: 1025, // >=1025   (>1024)
        lg: 1441, // >=1441   (>1440)
        xl: 1921 // >=1921   (>1920)
    }
}

export default breakpoints
