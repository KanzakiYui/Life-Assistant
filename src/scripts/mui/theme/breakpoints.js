/**
 * xs = 0, sm = 600 are default setting, while 1025, 1441, 1921 are
 * workaround to present >1024, >1440, >1920
 */
const breakpoints = {
    values: {
        xs: 0,
        sm: 600,
        md: 1025,
        lg: 1441,
        xl: 1921
    }
}

export default breakpoints
